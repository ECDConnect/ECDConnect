version: '3.3'
services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: ecdconnect
      POSTGRES_PASSWORD: admin
    ports:
      - '5432:5432'
    volumes:
      - './_data:/var/lib/postgresql/data'
      - '$PWD/:/tmp'