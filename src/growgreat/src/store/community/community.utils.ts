import { ThunkStateStatus } from '../types';
import { CommunityState } from './community.types';

export const handleEarnPointsInfo = (
  state: CommunityState & ThunkStateStatus,
  tab: string,
  locale: string
) => {};
