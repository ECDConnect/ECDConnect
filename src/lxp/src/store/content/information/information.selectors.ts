import { RootState } from '@/store/types';
import { MoreInformation } from '@ecdlink/graphql';

// export const getMoreInformationSelector = (state: RootState): MoreInformation[] =>
//   state.informationData.information || [];
