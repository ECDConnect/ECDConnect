import { Typography } from '@ecdlink/ui';

export const NotificationsView = () => {
  return (
    <div>
      <div>
        <Typography
          type={'h4'}
          color={'white'}
          text={'There are not notifications'}
        />
      </div>
    </div>
  );
};
