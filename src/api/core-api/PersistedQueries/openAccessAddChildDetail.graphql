query openAccessAddChildDetail($token: String) {
  openAccessAddChildDetail(token: $token) {
    child {
      firstname
      surname
      groupName
      userId
    }
    practitoner {
      firstname
      surname
      phoneNumber
    }
    accessToken
  }
}
