query GetYearPointsView($userId: UUID!) {
  yearPointsView(userId: $userId) {
    total
    monthSummary {
      month
      total
      activityDetail {
        activity
        timesScored
        pointsTotal
      }
    }
  }
}
