query GetAllUserConsent($createdUserId: UUID) {
  GetAllUserConsent(where: {
    and: [{
      createdUserId: { eq: $createdUserId }
    }]
  }) {
    id
    isActive
    consentId
    consentType
    userId
    createdUserId
    insertedDate
  }
}
