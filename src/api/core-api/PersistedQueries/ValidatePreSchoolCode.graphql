query ValidatePreSchoolCode($preSchoolCode: String!) {
  validatePreSchoolCode(preSchoolCode: $preSchoolCode) {
    id
    name
    userId
    siteAddress {
      id
      name
      addressLine1
      addressLine2
      addressLine3
      postalCode
      ward
    }
    user {
      principalObjectData {
        principalHierarchy
      }
    }
  }
}
