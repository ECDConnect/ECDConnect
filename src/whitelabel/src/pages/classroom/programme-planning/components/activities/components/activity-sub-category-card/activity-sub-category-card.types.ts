import { ProgressTrackingSubCategoryDto } from '@ecdlink/core';
import { ComponentBaseProps } from '@ecdlink/ui';

export interface ActivitySubCategoryCardProps extends ComponentBaseProps {
  subCategory: ProgressTrackingSubCategoryDto;
}
